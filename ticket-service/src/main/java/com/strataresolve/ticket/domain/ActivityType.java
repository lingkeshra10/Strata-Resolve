package com.strataresolve.ticket.domain;

/**
 * Enumeration of activity types that appear in a ticket's activity history.
 */
public enum ActivityType {
    STATUS_CHANGE,
    COMMENT,
    ASSIGNMENT,
    ATTACHMENT_UPLOAD
}
